# Benchmark task fixture. The harness copies this into a fresh scratch dir
# and feeds `prompt` to `opencode run`. Keep it representative of a normal
# coding task: read, write, edit, test.

name = "math-lib"
prompt = """
Create a small Python module src/mathlib.py implementing:
- gcd(a, b) using Euclid's algorithm
- fibonacci(n) returning the n-th number
- is_prime(n) primality check

Then write tests in test_mathlib.py and run them with pytest.
Fix any bugs you find. Do not write a README or extra files.
"""
